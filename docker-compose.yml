
services:
  django:
    build: ./django-app
    ports:
      - "8000:8000"
    volumes:
      - ./django-app:/app
    environment:
      - DEBUG=True
      - DJANGO_SETTINGS_MODULE=backend.settings
    depends_on:
      - react

  react:
    build: ./react-app
    ports:
      - "3000:3000"
    volumes:
      - ./react-app:/app
      - /app/node_modules
    stdin_open: true
    tty: true
